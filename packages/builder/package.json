{
  "name": "@plated/builder",
  "version": "0.0.0",
  "description": "Electron visual builder for Plated",
  "main": "./dist/electron/main.js",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@plated/types": "workspace:*",
    "@plated/generator": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^22.0.0",
    "electron": "^30.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
